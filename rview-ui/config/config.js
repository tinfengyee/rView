let r-version = '1.0.0'
let u-version = '1.4.8';

export default {
	v: r-version,
	version: r-version,
  u-v: u-version,
  u-version: u-version,
	// 主题名称
	type: [
		'primary',
		'success',
		'info',
		'error',
		'warning'
	]
}